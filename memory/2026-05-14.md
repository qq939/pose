# 2026-05-14

- 用户要求修复 Docker 内部部署的 8082 YOLO Pose Web App：手机端访问、检测结果黑屏、Python venv 缺失、`user_start.sh` 启动脚本。
- 设计为：Web 服务先启动，Python venv 后台安装；安装期间 8082 首页显示安装进度，`/api/setup-status` 返回 JSON 状态，安装完成后自动显示正常页面。
- 修复黑屏：前端 canvas 在没有检测结果或检测服务还没返回时继续绘制源视频帧；Python 视频处理进度改写 stderr，stdout 只保留最终 JSON。
- 注意提交时不能包含 `.venv/`、`node_modules/`、`uploads/`、`dataset/`、`logs/`。
- 用户追加要求摄像头 FPS 控件实时控制摄像头输出。实现思路：rAF 采集最新帧进入 5 张缓存池，按 `1 / fps` 秒消费最新帧输出到结果画布并发起检测，旧帧丢弃；默认 1 FPS。
- 用户反馈状态一直显示“检测服务准备中”。排查本地 `/api/status` 已 `pythonReady: true`，假摄像头检测接口返回 200，判断是前端曾经捕获错误后没有在成功检测帧清回“检测中...”。已计划修复成功帧状态恢复。
- 用户反馈“检测服务异常：stream worker exited code 1”。排查发现 stream worker 退出会被 Node 原样返回前端；已改为 Python stream 每帧捕获异常并返回 JSON，Node 在 worker 掉线时自动回退到单帧检测，避免 UI 直接暴露 worker 退出码。
- 用户反馈视频处理 `Processing failed` 信息不够，并要求视频 Pose 检测结果可下载。实现：`logs/run.log` 增加 `[process-video]` debug 日志，成功结果写入 `uploads/results/*.json`，前端显示下载按钮。
- 用户再次反馈最近上传视频仍失败。排查 Docker 容器 `18084-pose` 的真实日志，发现用户上传的 `56MB / 1920x1080 / 1321帧 / 30FPS` MP4 在 Python 启动时失败；根因是容器 `.venv` 装了 GUI 版 `opencv-python`，导入 `cv2` 报 `libxcb.so.1` 缺失，同时旧状态 JSON 误报 ready。
- 修复：`ensure_python_env.sh` 安装后强制卸载 GUI OpenCV 并安装 `opencv-python-headless`，验证成功后写 `logs/python-ready.ok`；`server.js` 必须看到 ready marker 才认为 Python 就绪，避免旧 ready 状态误导页面。
- 同步优化视频处理默认抽帧：前端上传检测默认传 `skipFrames=-1`，Python 按视频真实 FPS 自动抽成约 1 FPS，并在 stderr 写进度日志。复测同一 56MB 视频成功，1321 输入帧抽样 45 帧，约 10.8 秒完成，结果 JSON 可通过 `/results/...json` 下载。
- 用户确认这是超时/无反馈问题，要求超时时间 10 分钟且实时显示进度。实现：`/api/process-video` 支持前端传入 `requestId`，服务端维护内存进度表并提供 `/api/process-progress/:requestId`；前端上传处理时每秒轮询进度，显示已处理采样帧和百分比。服务端 `VIDEO_PROCESS_TIMEOUT_MS` 默认 600000ms，超时后终止 Python 并返回 504。已用同一 56MB 视频验证进度从 1%、2%、22%、44%、67%、89% 到 100%，最终结果可下载。
- 用户希望 FPS 控件默认 10，且上传视频抽帧也随这个 FPS 控件。实现：根页面 FPS 默认 `10 FPS`，上传视频传 `targetFps`；服务端将 `targetFps` 传给 Python；Python 在 `skipFrames=-1` 时按视频真实 FPS 自动换算为目标 FPS。React `/yolo/` 入口同步新增 `targetFps` 状态和检测 FPS 控件，摄像头实时检测节流也使用该值。验证小视频返回 `sampling_rate: target 10 fps`。
- 用户要求下载结果改为标注了 pose 节点的视频，并增加手关节标注。实现：`pose_detector.py` 在视频处理时同步输出 annotated MP4，绘制 YOLO 17 点人体骨架；可选启用 MediaPipe Hands 绘制 21 点手部骨架和指尖编号。服务端返回 `resultVideoPath/resultVideoFilename`，根页面和 React 页面下载按钮优先下载标注视频，JSON 继续保留作调试。验证小视频返回 MP4，`/results/*.mp4` 为 `200 OK` 且 `Content-Type: video/mp4`。
- 排查 `https://sadsad.fun` 手机无法访问：本地 `localhost:8082` 和 Docker `localhost:18084` 均 200；frpc 的 `mac18084` 远端连接也能到服务，`http://8.130.124.108:18084/api/status` 返回 running。但 `sadsad.fun` DNS 解析到 `198.18.5.62`，HTTP 返回阿里云 ICP 拦截页，HTTPS 在 TLS 握手阶段 reset；强制 SNI 到 `8.130.124.108` 也 reset。结论：这是域名 DNS/备案/nginx TLS server_name 配置问题，不是 Web App 或容器端口问题。
